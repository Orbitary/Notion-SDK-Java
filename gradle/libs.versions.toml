[versions]
bits = "0.0.+"

[libraries]
bits-api = { group = "xyz.bitsquidd.bits", name = "api", version.ref = "bits" }
okhttp = { group = "com.squareup.okhttp3", name = "okhttp", version = "5.3.0" }


[plugins]
bit-convention = { id = "xyz.bitsquidd.convention", version = "1.0.+" }
