rootProject.name = "Notion-SDK"

pluginManagement {
    repositories {
        mavenLocal()

        maven { url = uri("https://repo.bitsquidd.xyz/repository/bit/") }

        gradlePluginPortal()
    }
}
