rootProject.name = "notion-sdk-java"

pluginManagement {
    repositories {
        mavenLocal()

        maven { url = uri("https://repo.bitsquidd.xyz/repository/bit/") }

        gradlePluginPortal()
    }
}
